# Slipstream v0.2.34 Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. If that skill is unavailable, execute locally with the same checkpoints and fail-closed gates.

**Goal:** Publish the validated dual-engine Tools/JSON contract and oMLX safety fixes as Slipstream v0.2.34 with tested DMG and ZIP assets plus updated GitHub Pages.

**Architecture:** `Schero94/slipstream` is the canonical product repository. Port the already-tested files from the LLM-BOOM mirror, keep engine-specific capabilities scoped, and stage the matching oMLX runtime tree into the bundle. External publication happens only after local source, resource, build, and install gates pass.

**Tech Stack:** Vanilla JavaScript, Node contract tests, Tauri 2, Rust/Cargo, shell bootstrap, Python/oMLX/MLX, GitHub CLI, GitHub Releases, GitHub Pages.

---

### Task 1: Port the common Chat contract with a failing canonical test

**Files:**
- Create: `app/scripts/test_cross_engine_contract.mjs`
- Create: `app/scripts/test_api_parity_ai.mjs`
- Modify: `app/dist/app.js`
- Modify: `app/dist/index.html`

**Step 1: Add the canonical static tests**

Copy the validated pure Node contracts from the LLM-BOOM mirror, keeping their
root resolution relative to `app/scripts`.

**Step 2: Run tests before implementation**

Run:

```bash
node app/scripts/test_cross_engine_contract.mjs
node app/scripts/test_api_parity_ai.mjs
```

Expected: the cross-engine test fails because Metal still returns before adding
tools/response_format; the request-shape fixture passes independently.

**Step 3: Port the validated UI diff**

Apply only the measured delta from `LLM-BOOM/apps/peregrine-control/dist`:

- common preference keys with legacy fallback;
- common Tools/JSON controls and schema persistence;
- no Metal early return in `buildChatRequestBody`;
- `pgrnProfileForStart()` and incompatible-running-profile preflight;
- MLX/VLM attachment scoping unchanged.

**Step 4: Run the frontend gate**

Run every `app/scripts/test_*.mjs`; expect all scripts to pass.

**Step 5: Commit**

Commit only the two production files and new tests as
`feat: unify local coding contract`.

### Task 2: Port the bounded MLX profile and cache-safety fix

**Files:**
- Modify: `app/src-tauri/src/mlx.rs`
- Modify (bundle resource, ignored): `app/src-tauri/resources/omlx-pgrn/omlx/pgrn/profile.py`
- Modify (bundle resource, ignored): `app/src-tauri/resources/omlx-pgrn/omlx/pgrn/store.py`
- Create: `app/scripts/test_omlx_contract_resources.py`

**Step 1: Add a resource contract test**

The test imports the staged profile without MLX, asserts
`contract=(2048,1024,16)`, and statically checks that store eviction receives the
active-bank preserve set.

**Step 2: Run the test before implementation**

Expected: fail because the canonical staged profile lacks `contract` and store
lacks active-bank preservation.

**Step 3: Apply the validated profile/store diff and Rust allowlist**

Keep `contract` internal; Settings still expose only balanced/quality/fast.

**Step 4: Run focused Python and Rust tests**

Run the new resource test and `cargo test --manifest-path app/src-tauri/Cargo.toml`.
Expected: all pass, including the new Rust contract-profile test.

**Step 5: Commit tracked changes**

Commit `mlx.rs` and the resource contract test. The ignored staged Python files
remain release inputs and are verified separately before bundling.

### Task 3: Make the MLX runtime reproducible and grammar-complete

**Files:**
- Add existing text resources under `app/src-tauri/resources/omlx-pgrn/`
- Create: `requirements-mlx-grammar.txt`
- Modify: `bootstrap_mlx_runtime.sh`
- Modify: `requirements-mlx-runtime.txt`
- Modify: `.gitignore` only if needed to keep text resources tracked while
  retaining the large `omlx/` tree and dylibs ignored.

**Step 1: Add a failing bootstrap/resource test**

Extend `test_omlx_contract_resources.py` to require the pinned grammar file,
upgrade-required state, no-deps install, and metadata verification.

**Step 2: Run the test before implementation**

Expected: fail because the grammar requirements file and upgrade path are absent.

**Step 3: Port the validated bootstrap and requirements**

Pin `xgrammar==0.2.3` and `apache-tvm-ffi==0.1.11`; install with `--no-deps` so
the oMLX torch stub remains in use.

**Step 4: Verify runtime and staged parity**

Run `bash -n`, bootstrap `verify`, version metadata checks, and byte comparisons
against the validated LLM-BOOM staged inputs.

**Step 5: Commit**

Track all required small text resources and tests as
`fix: package structured MLX runtime`.

### Task 4: Version, release notes, and GitHub Pages

**Files:**
- Modify: `app/src-tauri/Cargo.toml`
- Modify: `app/src-tauri/Cargo.lock`
- Modify: `app/src-tauri/tauri.conf.json`
- Modify: repository `Cargo.lock` if package metadata changes
- Create: `docs/RELEASE_0.2.34.md`
- Modify: `docs/index.html`
- Modify: `README.md` if asset wording is inaccurate

**Step 1: Bump every product version to 0.2.34**

Use Cargo metadata/update rather than hand-editing unrelated lock entries.

**Step 2: Write evidence-bounded release notes**

Include 22 UI, 191 M3, 83 Rust, 14 llama, and 810+3 oMLX validation counts as
pre-port evidence; list canonical rerun counts separately. Include Q5's exact
qualified knobs and Granite's tool-quality caveat.

**Step 3: Update Pages**

Add a concise v0.2.34 section explaining common Tools/JSON and bounded MLX
structured requests. Keep download links pointed at `/releases/latest`.

**Step 4: Run page/static checks**

Validate HTML references, version occurrences, and Git whitespace hygiene.

**Step 5: Commit**

Commit as `release: prepare Slipstream 0.2.34`.

### Task 5: Run the complete local release gate

**Step 1: Run all canonical Node tests**

Expected: every script passes.

**Step 2: Run Rust/Cargo tests**

Run workspace tests and the app manifest tests. Expected: zero failures.

**Step 3: Re-run LLM-BOOM source gates**

Re-run the cross-engine Node contract, M3 pytest tree, llama PGRN CTest suite,
and relevant oMLX grammar/tool block. Expected: the previously qualified counts
or higher, with zero failures.

**Step 4: Verify bundle inputs**

Require executable llama-server/pgrn-convert, staged oMLX package, dylib, scripts,
and grammar requirements. Record sizes and hashes.

**Step 5: Stop on any red gate**

Do not push, tag, or create a release.

### Task 6: Build and locally verify release artifacts

**Step 1: Build the release**

Run `cargo tauri build` from `app/src-tauri`. Expected: `.app` and `.dmg` for
0.2.34.

**Step 2: Create the ZIP**

Use `ditto -c -k --sequesterRsrc --keepParent` on the built `.app`.

**Step 3: Inspect bundle contents and versions**

Check Info.plist, resource presence, executable architectures, sizes, and SHA-256.

**Step 4: Install safely**

Stop only Slipstream-owned processes, preserve the existing app as a temporary
backup, install with `ditto`, and confirm version 0.2.34. Restore on failure.

**Step 5: Run lightweight launch/static smoke**

Launch the app, confirm its process/window initialization, then terminate it
cleanly. Do not start a heavy model for this release smoke.

### Task 7: Publish and verify GitHub Release and Pages

**Step 1: Final source check**

Ensure only intentional untracked/ignored bundle inputs remain and commits are
ahead of `origin/main` with no failed gates.

**Step 2: Push main**

Push and verify remote main SHA equals local HEAD.

**Step 3: Tag**

Create annotated `v0.2.34`, push it, and verify the tag target.

**Step 4: Create GitHub Release**

Attach both DMG and ZIP plus SHA-256 lines in the release body.

**Step 5: Verify external state**

Confirm release assets are downloadable, GitHub Pages status is `built`, the
latest docs commit equals the release commit, the live page contains v0.2.34,
and `/releases/latest` resolves to v0.2.34.

**Step 6: Report completion**

Return public links, commit/tag SHAs, asset hashes/sizes, test totals, installed
version, Pages status, and any honest limitations.
